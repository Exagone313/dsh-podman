# disable dsh telemetry
- id: session-telemetry-otel
  disabled: true

# expose on 0.0.0.0 rather than 127.0.0.1
- id: webserver
  config:
    host: '0.0.0.0'
    port: !!js ctx.webStartup.port ?? 3080
